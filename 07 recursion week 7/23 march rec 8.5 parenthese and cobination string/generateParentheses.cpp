#include<bits/stdc++.h>
using namespace std;
void solve(int n,vector<string>& ans,string out,int open,int close){
    if(open==0 && close==0)
    {
        ans.push_back(out);
        return;
    }
    if(i>0&&)
}
int main(){
    int n;
    cout<<"Enter the value of n"<<endl;
    cin>>n;
    vector<string> ans;
    string out;
    solve(n,ans,out,n,n);
    for(auto i: ans)
    cout<<i<<" ";

}